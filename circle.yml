# The deployment section should be excluded until
# it is explicitly included in module 5

deployment:
  dockerhub:
    branch: master
    commands:
      - curl -H "Content-Type: application/json" --data '{"build":true}' -X POST https://registry.hub.docker.com/u/mickish/pluralsight-docker-ci/trigger/16d901b4-b77f-4020-b34d-507ece0af680/

test:
  override:    
    - mocha
